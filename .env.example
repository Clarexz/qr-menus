# URL pública donde vive el sitio, sin slash final (ej. https://casa-milpa.vercel.app).
# El QR de /dashboard/qr apunta a `${NEXT_PUBLIC_SITE_URL}/menu`.
# Si se deja sin definir, se usa el host de la request actual (útil en local).
NEXT_PUBLIC_SITE_URL=
